- AppIcon 资源遵循 Apple 命名规范 `Icon-{size}@{scale}.png`，并通过 Contents.json 显式标注 idiom/size/scale 三元组，而非依赖文件名推断。
- Info.plist 使用 `$(PRODUCT_BUNDLE_IDENTIFIER)`、`$(EXECUTABLE_NAME)` 等构建期变量，使同一 plist 可复用至多个 Target。