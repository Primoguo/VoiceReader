- 所有 I/O 操作（loadProducts、purchase、restorePurchases）统一通过 SubscriptionManager.shared 暴露的异步方法调用，View 内不直接访问 StoreKit 产品对象进行购买逻辑。
- UI 状态变更包裹在 Task 中执行，并在开始前设置 isPurchasing = true 禁用按钮、结束后重置，配合 purchaseError 字符串集中显示错误信息。
- 可复用的小组件以私有函数形式定义（如 featureRow），返回 some View 并通过参数传入 icon/title/desc，避免内联重复布局代码。