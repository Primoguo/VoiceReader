- 跨标签共享的状态对象（如 SpeakerViewModel）统一在 ContentView 中以 @StateObject 创建，再通过参数注入到各子 View，避免重复初始化
- 全局错误弹窗集中由 ContentView 顶层 .alert(item: ErrorHandler.shared.currentAlert) 订阅，子 View 仅通过 ErrorHandler.shared 触发