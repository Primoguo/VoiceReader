- 所有异步 IAP 操作使用 async/await 而非回调，并通过 `@MainActor` 保证状态更新在主线程执行。
- 对外暴露的可观察状态统一使用 `@Published var` 声明，UI 直接绑定这些属性。
- StoreKit 交易结果一律经 `checkVerified(_:)` 包装处理，将 `.unverified` 转换为统一的 `StoreError.failedVerification`。
- 公开方法遵循「设置 isLoading → defer 重置」模式，确保异常路径也能恢复加载状态。