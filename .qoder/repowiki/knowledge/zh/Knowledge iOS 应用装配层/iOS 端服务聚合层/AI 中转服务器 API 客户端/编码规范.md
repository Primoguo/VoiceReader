- 对外 API 以 async throws 形式暴露，失败一律抛出 `ServerAPIError` 而非返回 Optional。
- HTTP 响应先由 `validateResponse` 按状态码分类，再交由具体 post 重载做 JSON 反序列化和字段抽取。
- 请求体统一使用 `[String: Any]` 字典 + `JSONSerialization.data(withJSONObject:)` 编码，避免引入第三方 JSON 库。
- 服务端地址集中定义在类级静态常量 `baseURL`，并通过 TODO 注释提示部署后替换。