- 枚举统一遵循 `String, Codable, CaseIterable` 协议，并通过 computed property（如 `displayName`、`iconName`、`colorScheme`）暴露 UI 展示信息。
- 可持久化对象实现 `Codable`，并通过静态工厂/实例方法成对提供 `toJSON()` / `fromJSON(_:)` 或 `save*` / `load*` 形式的序列化接口。
- SwiftData 实体中避免直接存储复杂类型，改用 `rawValue` 字符串字段配合同名计算属性进行类型转换（见 `Document.fileType`）。